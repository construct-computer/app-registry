{
  "name": "construct-app-registry",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute app-registry --file=src/schema.sql",
    "prepare": "node scripts/stub-registry.mjs"
  },
  "dependencies": {
    "@construct-computer/app-sdk": "^0.2.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^6.0.2",
    "wrangler": "^4.83.0"
  }
}
